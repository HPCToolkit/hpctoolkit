{
  "name": "ci.predeps-rocm5.1",
  "build": {
    "args": {
      "ROCM_VERSION": "5.1.3"
    },
    "context": "${localWorkspaceFolder}",
    "dockerfile": "${localWorkspaceFolder}/.ci.predeps/Containerfile.rocm"
  },
  "capAdd": [
    "SYS_ADMIN"
  ],
  "runArgs": [
    "--device=/dev/kfd",
    "--device=/dev/dri"
  ]
}
