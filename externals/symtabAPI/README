======================
SymtabAPI Build Notes
======================

Home: http://www.paradyn.org/
      http://www.dyninst.org/

Download: ftp://ftp.cs.wisc.edu/paradyn/releases/release7.0/symtab_src_7.0.1.tar.gz

License: LGPL

Prereqs: libdwarf libelf libxml2 libunwind

Now uses autoconf but not automake.
Does not support vpath build or parallel make.

export PLATFORM=platform
cd symtabAPI/src/dyninst
./configure CC=CC CFLAGS=flags CXX=CXX CXXFLAGS=flags  \
    --prefix=prefix  \
    --bindir=bindir  \
    --includedir=includedir  \
    --libdir=libdir  \
    --with-libelf-incdir=incdir  \
    --with-libelf-libdir=libdir  \
    ...
make

Some compilers don't support tr1/unordered_map.  In that case, disable
unordered_map in dyntypes.h via a conditional patch.

Make copies files to includedir, libdir, etc, so don't need make
install.

