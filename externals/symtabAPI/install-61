#!/bin/sh
#
#  SymtabAPI 6.1 install
#
#  $Id$
#

PKG_NAME=symtabAPI

. ../build-utils/configure.inc
pre_install
prefix="$symtabAPI_prefix"

# Symtab's 'make' already copies the install files.
# All we do here is reset the permissions.

reset_perms "$prefix"

post_install
